import React from 'react';

const styles = {
  background: {
    color:'blue'
  }
  
};

const Footer = () => (
  <footer style={styles.background}>
    <hr />
    <p>Footer</p>
  </footer>
);

export default Footer;
